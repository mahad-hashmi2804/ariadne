[package]
name = "movement"
version = "0.1.0"
edition = "2024"

[dependencies]
mujoco-rs = { workspace = true, features = ["auto-download-mujoco"] }
nalgebra = { workspace = true }
serde_json = { workspace = true }
serde = {version = "1", features = ["derive"]}
crossterm = "0.29"
